server:
  port: 9000

# spring:
#   cloud:
#     gateway:
#       globalcors:
#         cors-configurations:
#           '[/**]':
#             allowed-Origins: "https:localhost:4200"
#             allowed-Methods: "*"
#             # allowed-headers: "*"
#             # allow-credentials: true
            
      routes:
      - id: microservice_back
        uri: http://localhost:9001
        predicates: Path=/patient/**
        filters:
          - PrefixPath=/microservice_back/v1

